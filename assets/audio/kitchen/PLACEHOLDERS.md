Drop the real minigame SFX files in this folder with these names:

- slice_placeholder.wav
- bomb_placeholder.wav
- win_placeholder.wav

The code already calls them through `flame_audio` and safely no-ops until real files are added.
